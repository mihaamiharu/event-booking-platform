{
  "name": "ebp-spike-a-pbkdf2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SPIKE-A (ACC-001, NFR-001): PBKDF2-SHA256 CPU benchmark in a Worker isolate. Throwaway harness; S0 decides final /worker layout.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "bench": "node --experimental-strip-types --no-warnings src/run.ts",
    "test": "node --experimental-strip-types --no-warnings --test src/stats.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.129.0"
  }
}
