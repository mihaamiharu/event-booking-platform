{
  "name": "ebp-spike-b-d1-concurrency",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SPIKE-B (BKG-002, BKG-003): D1 batch atomicity + concurrency proof. Throwaway harness; S2/S5 own prod schema.",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "bench": "node --experimental-strip-types --no-warnings src/run.ts",
    "test": "node --experimental-strip-types --no-warnings --test src/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "wrangler": "^4.129.0"
  }
}
