# API contract tests (S1+)

`node:test` + TS HTTP runner against the deployed Worker + D1 (local first).
Each file boots its own `wrangler dev` (see `health.nfr-001.test.ts`).
S3 adds the provision/catalog/detail suites here.
